schema: ".tmp/schema.graphql"
documents: "./src/**/*.ts"
generates:
  src/lib/generated/graphql/possibleTypes.json:
    plugins: ["fragment-matcher"]
    config:
      module: commonjs
      useExplicitTyping: true
  src/lib/generated/graphql/:
    preset: client
    plugins: []
